# SentinelAI Frontend Environment

# Optional — if unset, production builds default to Render backend via next.config rewrites
# NEXT_PUBLIC_API_URL=https://sentinelai-lee4.onrender.com

# Local development (docker-compose sets this automatically)
NEXT_PUBLIC_API_URL=http://localhost:8000
